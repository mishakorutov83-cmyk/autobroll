# AutoBroll — заметки для Claude

## Интервью-Reels «Люди нашего места» (эталон — выпуск 1, Мовсар)

Эталонный монтаж собран скриптами из `docs/episodes/ep1/` (`build.py` — EDL, склейки, кадрирование, субтитры).
Новые выпуски делать по тому же шаблону, если пользователь не попросит иначе:

- 9:16, 1080×1920, H.264/AAC, 2:40–3:00, без музыки и без B-roll.
- Хук — самая сильная реальная реплика гостя (0–5 с), затем «как зовут/откуда» + lower-third
  (`titles`, kind `lower`), затем живой диалог: вопрос ведущего → ответ → реакция. Ведущего не вырезать.
- Кадрирование по лицам (детекция YuNet), зумы 1.06 / 1.16 / 1.28, punch-in внутри длинных клипов,
  длинные проводки камеры вырезать.
- Субтитры: `captionPreset: 'clean'`, шрифт Inter (public/fonts), до 2 строк, акцент 1–2 слова, цвет `#F2C14E`,
  `topPct` ≈ 63.5. Текст проверять после Whisper вручную.
- Звук: dynaudnorm на исходнике, в конце loudnorm −14 LUFS.
- Финал: ресторан/блюдо → end card «ЛЮДИ НАШЕГО МЕСТА / Новые истории людей из Китая» (3 с, `titles`, kind `end`).

### Пожелание на будущее (тестируется)
- Слегка ускорять речь: `speed` клипа 1.1–1.2. Пока не утверждено — перед финалом показать
  пользователю короткий тест на 1.1 и 1.15 и спросить.

### Скорость работы
- Пользователь получает файлы в чат только до 30 МБ — сразу делать превью ≤30 МБ (2-pass ~1.1 Мбит/с)
  рядом с полной версией, либо заливать полную на Google Drive.
- Не апскейлить весь исходник: апскейл/нормализацию делать только для выбранных фрагментов.
